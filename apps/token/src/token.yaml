runtime: nodejs12
instance_class: F1
service: token

entrypoint: node main.js

env_variables:
  TOKEN_CONNECTION_URL: 'https://token-dot-composer-intuit.uc.r.appspot.com'

automatic_scaling:
  max_instances: 3
  min_instances: 0
  min_idle_instances: 0

inbound_services:
  - warmup
