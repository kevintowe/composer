runtime: nodejs12
instance_class: F1
service: composer-realm-logic

automatic_scaling:
  max_instances: 3
  min_instances: 0
  min_idle_instances: 0
  max_concurrent_request: 50

inbound_services:
  - warmup
